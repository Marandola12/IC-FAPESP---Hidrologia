setwd("D:/LabTerra/Arquivos do meu projeto/Builds/caete_hidrology_water_retention/caete_hidrology_water_retention/Topsoil")
library(raster)
WS <- raster("S_WS.nc4")
WS <- raster("WS.nc4")
WS
library(ncdf4)
setwd("D:/LabTerra/Arquivos do meu projeto/Builds/caete_hidrology_water_retention/caete_hidrology_water_retention/Topsoil")
ncinf <- nc_open("WS.nc4")
lon <- ncvar_get(ncinf,"longitude")
lat <- ncvar_get(ncinf,"latitude")dim(lat)
var_array <- ncvar_get(ncinf,"WS")
lat <- ncvar_get(ncinf,"latitude")
aggregate(var_array,10,mean)
ncvar_def("res_WS", "%", list(lon,lat))
library(ncdf4)
library(raster)
setwd("D:/LabTerra/Arquivos do meu projeto/Builds/caete_hidrology_water_retention/caete_hidrology_water_retention/Topsoil")
WS <- raster("WS.nc4")
res_WS <- aggregate(WS, 10, mean)
ncinf <- nc_open("WS.nc4")
lon <- ncvar_get(ncinf,"longitude")
lat <- ncvar_get(ncinf,"latitude")dim(lat)
lat <- ncvar_get(ncinf,"latitude")
ncvar_def("res_WS", "%", list(lon,lat))
ncvar_def(res_WS, "%", list(lon,lat))
ncdim_def("WSres", "%", res_WS)
ncdim_def("WSres", "%")
dim(res_was)
dim(res_wss)
dim(res_ws)
dim(res_WS)
list(res_WS)
array(res_WS)
arr_res_WS <- array(res_WS)
ncdim_def("WSres", "%", arr_res_WS)
ncvar_def("WSres", "%", list(lon,lat))
a <- ncdim_def("WSres", "%", arr_res_WS)
ncvar_def("a", "%", list(lon,lat))
v_res_WS <- c(res_WS)
a <- ncdim_def("WSres", "%", v_res_WS)
v_res_WS
library(ncdf4)
library(raster)
setwd("D:/LabTerra/Arquivos do meu projeto/Builds/raster test")
